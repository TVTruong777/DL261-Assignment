# AI Usage Disclosure — Assignment 1

CO3133 — Group [GROUP_ID] — Semester 261
Assignment 1: Foundations of Deep Learning Pipelines and Architectures

This is the repository-level disclosure required by course rules. It should
stay consistent with the disclosures on the landing page, the Assignment 1
page, and the report.

## Tools used

| Tool / model | Version | Used by |
|---|---|---|
| `[e.g., ChatGPT]` | `[e.g., GPT-4o]` | `[member]` |
| `[e.g., Claude]` | `[e.g., Sonnet 4.5]` | `[member]` |
| `[e.g., GitHub Copilot]` | `[version]` | `[member]` |

## What AI was used for

- **Pipeline / boilerplate code** (`src/data.py`, `src/train.py`, etc.):
  `[e.g., "Scaffolded with AI assistance, then reviewed, tested, and modified
  by the group." or "Written by the group without AI assistance."]`
- **Model implementations** (`src/models/*.py`): `[state per-model if it
  differs — e.g., "Transformer patch embedding + positional encoding logic
  was discussed with an AI assistant to check correctness, but implemented
  by [member]."]`
- **Debugging**: `[e.g., "Used ChatGPT to diagnose a shape mismatch in the
  RNN classifier."]`
- **Report / documentation writing**: `[e.g., "Used Claude to help phrase the
  methodology section; all technical content is the group's own."]`
- **Experiment design / hyperparameter choices**: `[e.g., "None — chosen by
  the group based on validation performance." ]`

## What was NOT AI-generated

`[State plainly which parts are fully the group's own work — e.g., the
architecture design decisions for the CNN, the choice of row/column/patch
representations for the RNN and Transformer, the analysis in the report's
"Comparison and discussion" and "Error analysis" sections.]`

## Verification statement

`[e.g., "All AI-suggested code was read, run, and understood — including the
unit tests in tests/ — before being committed. The group takes full
responsibility for the correctness of this submission."]`
